
// Copyright 2019 The Chromium Authors. All rights reserved.
// Use of this source code is governed by a BSD-style license that can be
// found in the LICENSE file.

// This file is autogenerated by
//     java_cpp_enum.py
// From
//     ../../components/signin/core/browser/signin_metrics.h

package org.chromium.chrome.browser.signin;

import android.support.annotation.IntDef;

import java.lang.annotation.Retention;
import java.lang.annotation.RetentionPolicy;

@IntDef({
    SignoutReason.SIGNOUT_TEST, SignoutReason.SIGNOUT_PREF_CHANGED,
    SignoutReason.GOOGLE_SERVICE_NAME_PATTERN_CHANGED,
    SignoutReason.SIGNIN_PREF_CHANGED_DURING_SIGNIN, SignoutReason.USER_CLICKED_SIGNOUT_SETTINGS,
    SignoutReason.ABORT_SIGNIN, SignoutReason.SERVER_FORCED_DISABLE,
    SignoutReason.TRANSFER_CREDENTIALS, SignoutReason.AUTHENTICATION_FAILED_WITH_FORCE_SIGNIN,
    SignoutReason.USER_TUNED_OFF_SYNC_FROM_DICE_UI, SignoutReason.ACCOUNT_REMOVED_FROM_DEVICE,
    SignoutReason.SIGNIN_NOT_ALLOWED_ON_PROFILE_INIT,
    SignoutReason.FORCE_SIGNOUT_ALWAYS_ALLOWED_FOR_TEST, SignoutReason.NUM_PROFILE_SIGNOUT_METRICS
})
@Retention(RetentionPolicy.SOURCE)
public @interface SignoutReason {
  /**
   * The value used within unit tests.
   */
  int SIGNOUT_TEST = 0;
  /**
   * The preference or policy controlling if signin is valid has changed.
   */
  int SIGNOUT_PREF_CHANGED = 0;
  /**
   * The valid pattern for signing in to the Google service changed.
   */
  int GOOGLE_SERVICE_NAME_PATTERN_CHANGED = 1;
  /**
   * The preference or policy controlling if signin is valid changed during the signin process.
   */
  int SIGNIN_PREF_CHANGED_DURING_SIGNIN = 2;
  /**
   * User clicked to signout from the settings page.
   */
  int USER_CLICKED_SIGNOUT_SETTINGS = 3;
  /**
   * The signin process was aborted, but signin had succeeded, so signout. This may be due to a
   * server response, policy definition or user action.
   */
  int ABORT_SIGNIN = 4;
  /**
   * The sync server caused the profile to be signed out.
   */
  int SERVER_FORCED_DISABLE = 5;
  /**
   * The credentials are being transfered to a new profile, so the old one is signed out.
   */
  int TRANSFER_CREDENTIALS = 6;
  /**
   * Signed out because credentials are invalid and force-sign-in is enabled.
   */
  int AUTHENTICATION_FAILED_WITH_FORCE_SIGNIN = 7;
  /**
   * The user disables sync from the DICE UI.
   */
  int USER_TUNED_OFF_SYNC_FROM_DICE_UI = 8;
  /**
   * Android specific. Signout forced because the account was removed from the device.
   */
  int ACCOUNT_REMOVED_FROM_DEVICE = 9;
  /**
   * Signin is no longer allowed when the profile is initialized.
   */
  int SIGNIN_NOT_ALLOWED_ON_PROFILE_INIT = 10;
  /**
   * Sign out is forced allowed. Only used for tests.
   */
  int FORCE_SIGNOUT_ALWAYS_ALLOWED_FOR_TEST = 11;
  /**
   * Keep this as the last enum.
   */
  int NUM_PROFILE_SIGNOUT_METRICS = 12;
}
